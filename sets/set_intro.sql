-- set operators 
/*
unique
except
union all
interset
*/
-- what are set operators
/*
sql set operations combines the results of multiple queries into a single result set
the difference between sets and joins
join ---
joins combines tables column by column
set combine the table row by rows
set goes and appened all rows with column having A and b

it is opposite of joins  we have a same column
*/
